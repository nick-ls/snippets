/**
 * Performs the javascript xor operation on two numbers and returns the result (using built in xor operator)
 * @param {Number, BigInt} a - The first number
 * @param {Number, BigInt} b - The second number
 */
function xor(a, b) {
    return a ^ b;
}
